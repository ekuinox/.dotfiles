# リポジトリ付随物（home に展開しない）
README.md
CLAUDE.md
docs
docs/**

# Windows 専用。他 OS では無視する
{{ if ne .chezmoi.os "windows" }}
Documents
Documents/**
.claude/init_env.sh
# WezTerm 設定は Windows ネイティブアプリ用（WSL 側の ~ には展開しない）
.wezterm.lua
{{ end }}

# nix/home-manager は Windows 非対象。Windows でのみ無視する
{{ if eq .chezmoi.os "windows" }}
.config/home-manager
.config/home-manager/**
.config/nix
.config/nix/**
{{ end }}
