# リポジトリ付随物（home に展開しない）
README.md
CLAUDE.md
docs
docs/**

# PowerShell プロファイルは Windows 専用。他 OS では入れ物ごと無視する
{{ if ne .chezmoi.os "windows" }}
Documents
Documents/**
.claude/init_env.sh
{{ end }}

# nix/home-manager は Windows 非対象。Windows でのみ無視する
{{ if eq .chezmoi.os "windows" }}
.config/home-manager
.config/home-manager/**
.config/nix
.config/nix/**
{{ end }}
